import { AbsoluteDirective } from './absolute.directive';

describe('AbsoluteDirective', () => {
  it('should create an instance', () => {
    const directive = new AbsoluteDirective();
    expect(directive).toBeTruthy();
  });
});
